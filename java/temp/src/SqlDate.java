import java.text.DateFormat;
import java.text.SimpleDateFormat;
import java.util.*;

public class SqlDate {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		/*// Make a new Date object. It will be initialized to the current time.
        Date now = new Date();
        String SDFormat = "yyyy:MM:dd HH:mm:ss";
        
        SimpleDateFormat fmt1 = new SimpleDateFormat(SDFormat);
        fmt1.setTimeZone(TimeZone.getTimeZone("GMT"));
        String fmt1Date = fmt1.format(now);   
        System.out.println(fmt1Date + "    "+ fmt1.toPattern());
        
        SimpleDateFormat fmt2 = new SimpleDateFormat(SDFormat);
        String fmt2Date = fmt2.format(now);   
        System.out.println(fmt2Date + "    "+ fmt1.toPattern());*/
		
		/*Set<String> val = new HashSet<String>();
		val.add("val1");
		val.add("val2");
		String[] valarr =  val.toArray(new String[0]);
		System.out.println (Arrays.asList(valarr));
		
		Set<String> newval = new HashSet<String> (Arrays.asList(valarr));
		System.out.println (newval);*/
		//SqlDate sd = new SqlDate ();
		//System.out.println(sd.getClass().getName());
		
		Integer val = null;
		if (val != 0) {
			System.out.println ("not equal");
		}
		else
			System.out.println("equal");
	}

}
